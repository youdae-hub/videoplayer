# API Mode: 'mock' (default), 'strapi', or 'custom'
VITE_API_MODE=mock

# Strapi CMS URL (required when VITE_API_MODE=strapi)
VITE_STRAPI_URL=http://localhost:1337

# Custom CMS server URL (required when VITE_API_MODE=custom)
VITE_SERVER_URL=http://localhost:4000
